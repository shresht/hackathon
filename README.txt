Install Anaconda3 from https://www.continuum.io/downloads

Create virtual environment with all requirements
------------------------------------------------
Open a terminal in the same directory "hackathon". Run the following command:
conda env create -f environment.yml

After all the packages are installed, run
------------------------------------------------
activate tensorflow
jupyter-notebook "Hackathon.ipynb"

shift+enter to go to the next cell

